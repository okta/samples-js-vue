{
  "name": "hackathon",
  "version": "0.1.0",
  "description": "Hackathon Proj",
  "scripts": {
    "acmehealth-server": "npm start --prefix acmehealth/",
    "resource-server": "node resource-server/server.js",
    "callout-service": "npm start --prefix callout-service/",
    "postinstall": "yarn install-acmehealth && yarn install-resource-server && yarn install-callout-service",
    "install-acmehealth": "cd acmehealth && yarn install",
    "install-resource-server": "cd resource-server && yarn install",
    "install-callout-service": "cd callout-service && yarn install && yarn init-app",
    "start": "npm run resource-server & npm run callout-service & npm run acmehealth-server"
  },
  "devDependencies": {},
  "bin": {},
  "private": true,
  "dependencies": {
    "npm-run-all": "^4.1.3"
  }
}
